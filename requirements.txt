# Core UI
PyQt5==5.15.11
qdarkstyle==3.2.3

# Deep learning / embeddings
torch>=2.1.0
transformers>=4.39.0
sentence-transformers>=2.5.1
accelerate>=0.26.0

# Vector search
faiss-cpu>=1.8.0
numpy>=1.24.0

# PDF parsing — PyPDF2 is the required fallback; PyMuPDF is the preferred fast backend
PyPDF2>=3.0.1
# PyMuPDF>=1.23.0   # optional — uncomment for faster/richer PDF extraction

# DOCX parsing — use python-docx, NOT the broken 'docx' package on PyPI
python-docx>=1.1.0

# Markdown / HTML helpers (optional but recommended)
markdown>=3.5.0
beautifulsoup4>=4.12.0
html2text>=2024.2.26

# Token counting (optional — falls back to word-count approximation if absent)
tiktoken>=0.6.0

# Optional GGUF local-inference backend
# llama-cpp-python>=0.2.0   # uncomment + install when using LOCAL_GGUF_MODEL
